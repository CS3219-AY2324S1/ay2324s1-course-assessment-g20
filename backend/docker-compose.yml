version: "3"
services:
  gateway:
    build: 
      context: .
      dockerfile: ./apps/gateway/Dockerfile
    depends_on:
      sql-migrations:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    ports:
      - ${API_GATEWAY_PORT}:${API_GATEWAY_PORT}
    networks:
      - backend
    env_file:
      .env
    environment:
      - AUTH_SERVICE_HOST=auth
      - QUESTION_SERVICE_HOST=question
      - RMQ_URL=amqp://guest:guest@rabbitmq:5672

  auth:
    build: 
      context: .
      dockerfile: ./apps/auth/Dockerfile
    depends_on:
      sql-migrations:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    networks:
      - backend
    env_file:
      .env
    environment:
      - AUTH_SERVICE_SQL_DATABASE_HOST=postgres
      - AUTH_SERVICE_SQL_DATABASE_PORT=5432
      - AUTH_SERVICE_SQL_DATABASE_USER=postgres
      - AUTH_SERVICE_SQL_DATABASE_PASSWORD=postgres
      - RMQ_URL=amqp://guest:guest@rabbitmq:5672

  question:
    build: 
      context: .
      dockerfile: ./apps/question/Dockerfile
    depends_on:
      sql-migrations:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    networks:
      - backend
    env_file:
      .env
    environment:
      - QUESTION_SERVICE_SQL_DATABASE_HOST=postgres
      - QUESTION_SERVICE_SQL_DATABASE_PORT=5432
      - QUESTION_SERVICE_SQL_DATABASE_USER=postgres
      - QUESTION_SERVICE_SQL_DATABASE_PASSWORD=postgres
  matching:
    build: 
      context: .
      dockerfile: ./apps/matching/Dockerfile
    depends_on:
      sql-migrations:
        condition: service_completed_successfully
    networks:
      - backend
    env_file:
      .env
    environment:
      - MATCHING_SERVICE_SQL_DATABASE_HOST=postgres
      - MATCHING_SERVICE_SQL_DATABASE_PORT=5432
      - MATCHING_SERVICE_SQL_DATABASE_USER=postgres
      - MATCHING_SERVICE_SQL_DATABASE_PASSWORD=postgres
  postgres:
    image: postgres:14.1-alpine
    restart: always
    volumes:
      - ./postgres-data:/var/lib/postgresql/data # Persist data between restarts
    env_file:
      .env
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5
    # expose port 5432 to be accessible from outside the container via 1111 on localhost
    expose:
      - 5432
    ports: 
      - 1111:5432
  sql-migrations:
    build: 
      context: .
      dockerfile: ./scripts/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      .env
    networks:
      - backend
    environment:
      - AUTH_SERVICE_SQL_DATABASE_HOST=postgres
      - AUTH_SERVICE_SQL_DATABASE_PORT=5432
      - AUTH_SERVICE_SQL_DATABASE_USER=postgres
      - AUTH_SERVICE_SQL_DATABASE_PASSWORD=postgres
      - QUESTION_SERVICE_SQL_DATABASE_HOST=postgres
      - QUESTION_SERVICE_SQL_DATABASE_PORT=5432
      - QUESTION_SERVICE_SQL_DATABASE_USER=postgres
      - QUESTION_SERVICE_SQL_DATABASE_PASSWORD=postgres
      - COLLABORATION_SERVICE_SQL_DATABASE_HOST=postgres
      - COLLABORATION_SERVICE_SQL_DATABASE_PORT=5432
      - COLLABORATION_SERVICE_SQL_DATABASE_USER=postgres
      - COLLABORATION_SERVICE_SQL_DATABASE_PASSWORD=postgres
  rabbitmq:
    image: rabbitmq:management
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    networks:
      - backend
    healthcheck:
      test: [ "CMD", "rabbitmqctl", "status"]
      interval: 5s
      timeout: 20s
      retries: 5

networks:
  backend:
    driver: bridge
